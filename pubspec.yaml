name: p3pathfinder
version: 0.0.1
description: P³ is an clustered pathfinder algorithm for calculating the flight range in 3d space based on geographical terrain and glide angle
dependencies:

dev_dependencies:
  test: ^1.5.3

